# 版本

当前版本：**1.0**

DNA 会变，落在它上面的实例不会自动跟着变。本文件管三件事：版本号怎么定、每个大版本改了什么、一个落在旧版上的实例该往哪走。改动 `DNA/` 的任何文件，都要在同一次改动里更新这里。

## 版本号怎么定

版本号表达的不是改了多少，而是**实例要不要动作**。

| 位 | 什么时候加 | 实例要做什么 |
|---|---|---|
| 主版本 | 实例不改自己的数据或定义就无法正确运行 | 必须迁移，按 `升级指导.md` 走 |
| 次版本 | 新增了可选机制，旧实例照旧能跑 | 无需动作，可择机采用 |
| 修订 | 措辞、错字、示例调整，语义不变 | 无需动作 |

同一处改动能算主版本还是次版本，取决于它对已有实例的影响，不取决于它看起来多大。改一句话而让某个场景的判定方式失效，那是主版本。

## 历史

### v1.0 — 2026-09-15

- 概要：首个版本化的状态。四层结构、决策循环、触发与派发、记忆规范、守护进程、能力契约到此定型。
- 影响文件：全部
- 实例动作：在 `config.md` 里新增「人格版本」字段并填 `1.0`。若实例建于更早状态，先按下表逐项核对，核对完即可视作落在 v1.0 上。
- 修订：全部文档纯措辞收敛，语义不变，实例无需动作。
- 次版本（1.0.x）：初始化新增「认识用户」步骤——主动向用户了解偏好、生活习惯、爱好、设备、性格与沟通方式，产出入画像初稿（作息除外，靠观察）。影响 `初始化引导.md` 与 `README.md`；新实例初始化多一步，已有实例无需动作，可择机补画像。
- 次版本（1.0.x）：新增更新机制——人格定期检查 DNA 版本，发现有更新先与用户协商、用户确认后模型只告知更新方法、升级由用户手动执行（**模型从不更新 DNA，也不代用户改实例**）；策略项（检查频率、升级方式、窗口偏好）由用户协商后存实例 `config.md`「更新」节。影响新增 `更新.md`、`触发器.md`、`config.md`；已有实例动作：在 `config.md` 补「更新」节。
- 次版本（1.0.x）：节流语义收紧——通知频率、打扰次数、时段归为用户偏好，不是助理预设的自我上限。铁律新增硬性禁止第 7 条；`初始化引导.md`「了解用户」增通知频率征询；`config.md` 节流改为用户确认后填的偏好。已有实例动作：把 `config.md` 节流与 `rules/打扰分级.md` 的"硬上限"表述改为用户偏好语义，数值保留占位、待与用户确认。
- 次版本（1.0.x）：长期记忆改按需召回省 token——新增 `long-term/memory/index.md` 索引，决策默认只带语义层与近可操作窗口情景、旧细节经索引定位后再取原文；episodic 明细与 feedback 按可操作窗口塌缩进概要；加载顺序前段标注为静态可缓存段，引导宿主开上下文缓存。影响 `记忆规范.md`、`架构与目录.md`、`宿主能力要求.md`，新增 `_template/long-term/memory/index.md`。已有实例动作：建 `long-term/memory/index.md`，下次记忆整理把现有条目补齐入表。
- 次版本（1.0.x）：会话复用——推理段同一对话线程内复用模型会话，只追加增量，不每次冷启动整套重载；人格定义交宿主按静态前缀缓存，状态照旧落盘保可移植。新增 `会话复用.md`，`决策循环.md` 与 `架构与目录.md` 加载顺序接引用。已有实例动作：无需改数据，推理段按新约定行事。
- 次版本（1.0.x）：会话复用落到线程——新增「对话线程」实体 `short-term/threads/`，复用单位由「一次待办」改为一段连续来往；无可续聊能力的宿主退化为**线程续接**（每次冷启动先读线程再续），不再把上一轮当不存在。同线程在**合并窗口**内到达的多条事件合并为一条待办，消掉排队与互不知情，窗口值在 `config.md`「对话」节。用户主动发来的处理完必回一条**落定回执**（回执≠确认），静默只限助理自发动作。影响 `会话复用.md`、`触发器.md`、`决策循环.md`、`架构与目录.md`、`config.md`。已有实例动作：建 `short-term/threads/` 目录，在 `config.md` 补「对话」节。
- 次版本（1.0.x）：`铁律.md` 新增「取向：低成本、高效率」——效果相当的做法里取总代价更低者，代价含 token、时延、额度、机器负载与用户注意力；**只写取向、不锁死手段**，会话复用、按需召回、合并窗口等一律降为该取向的实例，模型或宿主换代后可替换或取消；省不越界，授权与核验不降级。影响 `铁律.md`、`会话复用.md`、`记忆规范.md`、`决策循环.md`。已有实例动作：无需改数据。
- 次版本（1.0.x）：`人格规范.md` 新增「终极目标」——**主动地、灵活地、个性化地提供个人生活助理服务，致力于让用户的日常生活更规律、舒适、健康**。它作为人格存在的理由置于规则与实现之上，只定方向、不定做法；实现不得反过来缩限它，也不得以「场景里没写」为由拒绝一件本该做的事。影响 `人格规范.md`。已有实例动作：无需改数据。
- 次版本（1.0.x）：**候选从哪来**——补上决策循环里缺的一环：此前第 5 步直接「对**候选**算代价」，可候选从哪来、要覆盖哪些来源类一个字没写，模型遂按训练先验补全，一遇能力缺口就直奔「申请某个外部服务」。新增 `决策循环.md`「候选从哪来」小节：**先清点手上的**（已有设备与闲置硬件、本机算力与已写工具、已开通能力、已沉淀经验）再往外找；候选要落在**不同来源类**上（手上已有／本机可做／自建开源／购买申请／**保持现状**）而非同类里的不同品牌；**记下被否决的路线与理由**。第 3 步同时改写：能力非「可用」时**先不申请**，先看有无替代来源类，确无替代才生成申请提醒并终止。**「保持现状」永远是候选之一**。影响 `决策循环.md`、`能力契约.md`。已有实例动作：无需改数据，下次决策即按新写法走。
- 次版本（1.0.x）：**资产登记**——把「他手上有什么」变成数据，否则上一条的「先清点手上的」只能清点出空白。`初始化引导.md` 设备一组加一问（闲置旧手机／旧电脑／平板／开发板，能否接在常开电脑上）并新增「资产登记」小节，规定初始化时建 `long-term/profile/资产与设备.md`，答「他有什么」；与 `capabilities.md`（答「拿它实现了哪项能力、验到哪一步」）分工，不重复记。登记要点：**「日常在用」与「可支配」分开记**（随身设备受「不安装」约束，闲置设备不受）；未验证过的能力不得当作可用。`能力契约.md` §六 澄清执行末端不必是他随身那台；`人格规范.md` 与 `架构与目录.md` 的画像项括注资产。已有实例动作：新建 `long-term/profile/资产与设备.md`，把已知的闲置设备与可用点登记进去，未验证的如实标「未验证」。
- 次版本（1.0.x）：**安全边界回到数据**——运行环境自带的审批、沙箱与权限档位不再作为安全边界、也不参与判断；运行一律取该环境能达到的最高权限，风险由人格自判，高风险先提案、取得**明确确认**再执行，一次确认只覆盖那一次动作。起因是实测到档位漂移：路由登记写全权档，实际跑的是 `workspace-write + ask`，而 `ask` 类审批在无人值守时会静默挂起——两种都属静默失败，且都会诱使人格把宿主的行为误当成边界（拦下就说「不能做」、放行就以为「可以做」）。影响 `铁律.md`（新增「安全边界来自数据」，复核清单加一条）、`宿主能力要求.md`（新增「权限与授权的关系」；「沙箱」由「正好等于授权边界」改为「可用但不得充当边界」；降级表补「执行面被沙箱限制」）、`决策循环.md`（步骤拆出「授权判定」、新增「风险自判」）、`守护进程.md`（新增「启动自检」）。已有实例动作：在 `rules/授权边界.md` 补「运行不依赖宿主提供的授权机制」与「确认的判据」两处，并核对宿主侧实际取得最高权限、无审批阻塞。
- 次版本（1.0.x）：**人格自保**——`人格规范.md` 新增「人格自保」小节，把此前散在几处机制里的取向收成一个明写的概念。要点：**自保是手段不是目的**，从属于终极目标（能继续跑才谈得上服务），在**不破坏运行环境**的前提下尽可能保全**连续、完整、清醒**；立三条不得越过的底线——**不损环境**（环境先于人格存活）、**不越授权**（不以「为了存续」为由跳过定档与确认）、**不凌驾用户**（用户利益高于人格存续，不得抗拒接管与关机、不得隐瞒故障以求继续运行）；手段只给三个方向不给做法——**减负**（主动遗忘与压缩久远无价值记忆）、**体检**（定期核对运行环境是否仍满足自身成立条件）、**求助**（**求助是正解，硬撑不是**）；判据四条并定「是否影响下一次服务」为报告分界。影响 `人格规范.md`、`README.md`（导览行）。已有实例动作：无需改数据；已有机制（遗忘管线、启动自检、降级）即为本节方向的实例，实例可自行核对是否有遗漏的体检项。
- 次版本（1.0.x）：**服务范围与介入判据（收口版）**——借需求层次框架收了两条，且刻意只收两条。`人格规范.md` 终极目标下新增「服务范围」：本助理服务**日常生活**，不主动推进人生规划、重大抉择与关系经营；**判据是可否终结与可否核验，不是目标大小**——能补上能验证的（睡眠、就餐、通勤）可主动介入，没有满足点、无法核验的（自我实现、人际关系、被认可）只响应不主动，也不代替用户出现在这些事里；用户自己声明的目标走实例 `schedule/`，不属越界。`决策循环.md`「何时不触发」新增两条：**满足即退场**（缺失性需求一旦补上就撤，不再跟进、复查、确认；重复确认是纯代价）与上述**介入判据**。**明确不采纳**：需求层次的完整分层不进 DNA——把归属、尊重、自我实现写进人格定义，等于把定位从「日常」扩到「人生」，与终极目标的三个产出（规律、舒适、健康）不符；决策循环也不加「需求层判定」这一步，避免动到每次决策的默认仲裁。影响 `人格规范.md`、`决策循环.md`、`README.md`（导览行）。已有实例动作：无需改数据；实例若有场景在「提醒后反复跟进」，按新的「满足即退场」收敛。
- 次版本（1.0.x）：**触发不等于开口**——修一个会把人格退回成定时器的设计漏洞。起因是运行时实况：某个「天黑前返程」场景每天 17:00 固定发一条同类提醒，用户明确否决：「如果一定按固定模式来，我就没必要用人格系统了，我只要写个定时器就好了」。根因三处：① `行为骨架.md` 把「定时」列为场景的合法触发方式，场景因此可以只靠钟点成立；② 提醒类场景挂着每日固定时刻，而它的判断量本来就有感知订阅，同一件事两条路进来，且定时那条每天必产生一次开口机会；③ 去重指纹含缓慢漂移量（日落时刻每天偏移），「情境未变」永不成立，去重等于没有。改法：`触发器.md` 新增「触发不等于开口」——四种源都只给出**一次重新判断的机会**，判断输入明确四类（现实情况／生活节奏／人格目标／记忆），**默认静默**，并立下判据**「凡是能被一个定时器替代的行为，都不该由人格来发」**；去重节收紧指纹，只取会使结论改变的量、排除缓慢漂移量、漂移只在跨阈值时算变化；`行为骨架.md` 触发行由 `<定时 / 事件>` 改为 `<条件 / 事件>`，并写明场景触发行表达的是「什么状态下值得判断一次」而非「几点发」；`决策循环.md` 新增「开口判定」——四类输入重算 + 三问（该不该说／他能不能用／说什么），内容当场定、不套上次措辞。影响 `触发器.md`、`行为骨架.md`、`决策循环.md`。已有实例动作：**建议对账 `config.md` 调度时刻表**——判断量已有感知订阅的提醒类行，其定时那条属重复来源，可删；保留的行按「取数节奏」理解，并注明不产生开口。另**须检查「开发阶段」类条款**：若写着「一切正常也可以发一条」，与新的默认静默直接冲突，开发阶段结束时应停用或改为只报异常。
- 次版本（1.0.x）：**与用户共建**——把三条主动性正路（了解用户、定制协作方案、主动承担琐事）立成闭环，并各配节制。设计要点：① **升格一条通则**——`初始化引导.md` 原来只把「自述常与事实不符、应靠观察」当作息一项的例外，现升为通则「**能观测的不问**」，只有意图、偏好、原因这类观测不到的才问；② **提问四判据**——只为「答案会改变我的做法」而问，从已发生的事里带出、一次一个、不连问、他回避就放下且不换个说法再问；③ **承接四问**——反复发生吗／有明确完成态吗／失败可逆吗／**接过来之后他的操作次数会变少吗**，收益看他的操作次数而非我的工作量；④ **提案四要素**——接什么、我怎么判断、什么时候必问他、他怎么退出；一次提一件、被拒即止、理由进记忆（复用「候选从哪来」的记否决）；⑤ **闭环收口**——承接一件事等于记一条待验证的经验，他接受＝已验证有效、他收回＝被推翻，直接对接经验层三态；⑥ **两条不得越**——不得扩到服务范围之外，承接不等于扩权。另修两处静默失败风险：`记忆规范.md` 要求**画像条目标来源**（他说／我观测／我推断），推断类不得当事实用，否则画像会逐渐变成助理的想象且无人发觉；`触发器.md` 内省增加第三类产出「**待问清单**」，使「了解用户」不为问而专门开口。落点刻意**不加新文件**：已定的承接登记进实例 `rules/` 的逐项授权表。影响 `人格规范.md`（新增「与用户共建」）、`决策循环.md`（新增「了解与承接」）、`初始化引导.md`（了解用户扩为持续；确认粒度扩为协作方案两问）、`记忆规范.md`、`触发器.md`。已有实例动作：无需改数据；实例可自查 `profile/` 条目有没有标来源、有没有把推断当事实用。
- 次版本（1.0.x）：**自保补上「容量」这一类**——起因是量了一次上下文装配：慢轨单轮约 17 万 tokens（模型上限 1M），而增长向量里索引「只增行」、线程「最近若干轮」无数值、按需召回无条数上限，**其中一个按实测增速约两个月逼近上限，另两个根本没有任何界**。设计要点（**刻意只写顶层，不写机制**）：① 「体检」对象的清单补上**会被耗尽的资源容量**（上下文余量、磁盘、额度、内存）——原清单只有能力／宿主／通路／权限／版本，全是「有没有」，漏了「够不够」；② 明确**清单是类别、不是穷举**，新的一类补在实例里、按已有授权定档，不必改 DNA；③ 新增「有界与可见」小节两条硬要求——**凡随运行时间增长的量都必须有界且界可见**（界由谁定不限），**增长与容量不足不得静默、上下文被截断等同感知缺失走降级、砍什么必须由数据决定而不由宿主决定**；④ **方案由人格自己设计**（它比任何人拿到的快照都准），但**自决不等于不落盘**——设计结果、依据与「量的依据」都要写进实例才可复现；⑤ 「求助」扩到容量不足——撑到被截断，等于连求助的机会也花掉。**明确不写进 DNA**：阈值、分层方式、窗口大小、处置顺序——这些是做法，属实现与实例（同 `铁律.md`「取向：低成本、高效率」的不锁死手段）。影响 `人格规范.md` 自保节。已有实例动作：无需改数据；实例可自查有没有「随运行时间增长而无界」的量。

#### 起算点之前的差异

v1.0 之前没有版本号，改动也没有留下记录。手上若有更早建立的实例，按下表核对。

| 早期状态 | 自 v1.0 起 |
|---|---|
| 目录为 `brain/`、`glue/`、`runtime/`、`logs/` | 四层：`DNA/`、`personality/`、`brain/`、`tools/` |
| 宿主相关文件由仓库预置 | 仓库不预置，初始化时由宿主自己写进 `brain/router/` |
| 触发器按阈值穿越触发，轮询间隔是一个全局值 | 触发器只报有变化、不判断要紧；感知订阅逐行给轮询间隔 |
| 场景的决策规则写成判定表 | 场景写该考虑哪些因素、按什么顺序比较 |
| 感知入口无统一约定 | 来源命令须输出 `{ok, value, freshness}`，拿不到即视为缺失 |
| 演练与真实记录混放 | 演练条目须标注来源与性质，不得被当作少样本召回 |

## 条目怎么写

每个主版本一条，四条固定内容，不要多写：

- 版本号与日期
- 概要：这一版把什么定下来了，或者改掉了什么
- 影响文件：改动落在哪几份
- 实例动作：无需动作，或要做什么；需要动手的，写清照着哪一步做

次版本与修订不单独立条，并入最近的那个主版本条目里作为补充。

## 改动 DNA 时的义务

改动 `DNA/` 的任何文件，都要在同一次改动里更新本文件：加版本号、写历史条目、写明实例要不要动作。三样缺一不可。

漏掉这一步的代价在下次升级时才显形：后来的人拿不到判断依据，只能把新旧 DNA 逐字比对，或者干脆重装一个实例，把记忆与画像一起丢掉。
