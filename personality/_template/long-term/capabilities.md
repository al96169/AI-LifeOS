# 能力清单

> 骨架。抽象需求与降级规则见 `../../../DNA/能力契约.md`，这里只记实际选了哪家、配置到哪一步、密钥放在哪个变量名。

## 状态机

```
未申请 → 待申请 → 已申请 → 已配置(密钥/点位) → 可用
                          └─ 不可用（降级/失败）←──┘
```

处于「可用」之前，依赖它的场景一律走降级路径，不得臆造数据。

## 清单

| 能力 | 本环境实现 | 类型 | 状态 | 依赖场景 | 密钥变量 |
|---|---|---|---|---|---|
| | | | | | |

## 申请顺序

<按依赖关系排定顺序，把底座与最高价值场景排前面，涉及付款的排最后>

## 逐项配置

### 1 <能力名>

<申请步骤、验证方式、本环境注意事项>

## 记录要求

每项能力配置完成后在上表补上申请日期、点位引用与验证结果。密钥一律只写 `long-term/secrets.env`，本文件只存变量名。
